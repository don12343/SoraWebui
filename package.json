{
  "name": "sora-webui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "@headlessui/react": "^1.7.18" // Do not include comments in your actual JSON file.
    // other dependencies...
  },
  "devDependencies": {
    "@types/node": "^20",
    // other devDependencies...
  }
}
